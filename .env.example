# Copy to .env and fill in. Never commit the real .env.
API_KEY=
ENDPOINT=
DEPLOYMENT=
API_VERSION=2024-02-15-preview

# Grading client tuning (all optional, defaults shown)
MAX_RETRIES=5
REQUEST_TIMEOUT=90
CONNECT_TIMEOUT=20
MAX_CONCURRENCY=8
SLEEP_BETWEEN_REQUESTS=0.0
